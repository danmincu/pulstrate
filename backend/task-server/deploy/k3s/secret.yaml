apiVersion: v1
kind: Secret
metadata:
  name: task-server-secrets
type: Opaque
data:
  # Add base64-encoded secrets here
  # Example:
  # SomeApiKey: <base64-encoded-value>
stringData:
  # Add plain text secrets here (will be base64-encoded automatically)
  # Example:
  # SomeApiKey: "your-api-key-here"
